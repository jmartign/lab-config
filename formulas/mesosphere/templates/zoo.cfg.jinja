# Managed by Salt

maxClientCnxns=50
tickTime=2000
initLimit=10
syncLimit=5
dataDir=/var/lib/zookeeper
clientPort=2181

{%- for server, values in servers.items() %}
server.{{ values.id }}={{ server }}:2888:3888
{%- endfor %}
